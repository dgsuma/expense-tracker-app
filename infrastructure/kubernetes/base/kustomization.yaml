apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - db.yaml
  - api.yaml
  - web.yaml
  - redis.yaml
  - backup.yaml
  - ingress.yaml

# Image tags are set per-overlay (dev/prod) via the images transformer.
