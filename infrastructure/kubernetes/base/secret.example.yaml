# EXAMPLE secret — do NOT commit real values.
# In the target cluster, secrets come from an external secrets store
# (e.g. External Secrets Operator) or are applied manually / via Sealed Secrets.
# This file documents the required keys only.
apiVersion: v1
kind: Secret
metadata:
  name: expense-tracker-secrets
  namespace: expense-tracker
type: Opaque
stringData:
  POSTGRES_PASSWORD: "CHANGE_ME"
  JWT_SECRET_KEY: "CHANGE_ME_generate_a_long_random_string"
